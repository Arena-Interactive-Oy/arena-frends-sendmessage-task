name: SendSmartMessage_build_main

on:
  push:
    branches:    
      - main
    paths:
      - 'ArenaInteractive/**'
  workflow_dispatch:

jobs:
  build:
    uses: FrendsPlatform/FrendsTasks/.github/workflows/build_test_partner.yml@main
    with:
      workdir: ArenaInteractive
      